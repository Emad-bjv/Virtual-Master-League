# 🗺️ نقشه راه توسعه تخصصی (مستر لیگ مجازی)

این سند مراحل پیاده‌سازی پروژه را از نقطه صفر تا انتشار اپلیکیشن در مایکت/بازار به صورت کاملاً تفکیک‌شده و تخصصی نشان می‌دهد. فازبندی بر اساس پیش‌نیازهای فنی و اولویت‌های تجاری چیده شده است.

---

## 🏗️ فاز ۱: زیرساخت هسته و سیستم مربیگری (Core & Manager System)
**هدف:** ایجاد شالوده اصلی سایت، سیستم احراز هویت و مهم‌ترین ابزار مربیان (چیدن ترکیب).

### بخش بک‌اند (Django)
*   **پیکربندی پایگاه داده:** طراحی مدل‌های ارتباطی (Models) برای تیم‌ها، بازیکنان، مسابقات و مربیان در PostgreSQL.
*   **احراز هویت OTP:** پیاده‌سازی سرویس پیامکی (Kavenegar) برای ورود یکبار مصرف و قفل کردن هر تیم به یک شماره موبایل.
*   **API ترکیب (Game Plan):** ساخت End-point برای دریافت و ذخیره مختصات دقیق (X, Y) بازیکنان روی زمین و تاکتیک‌های انتخابی.
*   **زیرساخت پس‌زمینه:** راه‌اندازی Celery و Redis برای صف‌بندی تسک‌ها (آینده‌نگری برای فاز ۲).

### بخش فرانت‌اند (React)
*   **معماری پایه:** نصب React (Vite)، Tailwind CSS و تنظیمات اولیه PWA (برای خروجی اپلیکیشن در آینده).
*   **داشبورد مربی:** ساخت رابط کاربری اولیه برای مشاهده وضعیت تیم و لیست بازیکنان.
*   **زمین تاکتیک (Game Plan Clone):**
    *   پیاده‌سازی `dnd-kit` برای قابلیت Drag & Drop.
    *   طراحی شماتیک زمین PES و تبدیل مختصات وب به پست‌های واقعی (CF, AMF, CMF).
    *   پیاده‌سازی فرم‌های Advanced Instructions مشابه PES.

---

## ⚙️ فاز ۲: سیستم عملیاتی و اقتصاد پایه (Operations & Economy)
**هدف:** خودکارسازی وظایف ادمین، راه‌اندازی کیف پول دلاری و پرداخت واقعی.

### بخش بک‌اند (Django)
*   **پنل ادمین اختصاصی:** ساخت فرم‌های سریع برای ثبت گل‌ها، پاس گل‌ها، کارت‌ها و مصدومیت‌ها توسط ادمین در پایان هر بازی.
*   **تراکنش‌های امن (Atomic):** پیاده‌سازی کیف پول دلار مجازی با مکانیزم‌های قفل‌گذاری برای جلوگیری از هک شدن موجودی.
*   **درگاه پرداخت:** اتصال به API زرین‌پال، ساخت بخش درگاه و تخصیص اتوماتیک بسته‌های دلاری پس از پرداخت موفق.
*   **موتور پردازش استقامت:** کدنویسی فرمول $\Delta S$ (با در نظر گرفتن سن، توان PES و بازی متوالی) که توسط Celery بعد از هر بازی به صورت خودکار محاسبه می‌شود.

### بخش فرانت‌اند (React)
*   **جدول رده‌بندی و نتایج:** طراحی صفحات جدول لیگ با آپدیت در لحظه.
*   **فروشگاه بسته‌ها (Store):** طراحی UI جذاب برای خرید بسته‌های برنز، نقره و طلا با پول واقعی.
*   **تاریخچه مالی:** نمایش تراکنش‌ها و هزینه‌ها/درآمدهای تیم به مربی.

---

## 🎲 فاز ۳: سیستم نقل‌وانتقالات و گاشا (Transfers & Gacha)
**هدف:** پیاده‌سازی جذاب‌ترین بخش درآمدزای بازی یعنی پک اوپنینگ و بازار نقل و انتقالات.

### بخش بک‌اند (Django)
*   **موتور پک اوپنینگ (Gacha Engine):**
    *   برنامه‌نویسی Drop Rate های شفاف (Rare, Epic, Legendary).
    *   پیاده‌سازی سیستم Pity (شمارنده ۹ پک ناموفق = دهمی قطعی).
*   **بازار نقل‌وانتقالات:** سیستم ثبت پیشنهاد (Bid) مربیان به یکدیگر و سیستم آزادسازی اتوماتیک بازیکنان مازاد تیم‌های بالای ۲۵ نفر.
*   **فرمول رشد/افت:** کدنویسی محاسبه اتوماتیک میانگین نمرات هر ۶ هفته بازی و اعمال ارتقا (+2) یا افت (-2) بازیکنان.

### بخش فرانت‌اند (React)
*   **انیمیشن‌های پک اوپنینگ:** استفاده از **Framer Motion** برای ساخت افکت‌های جذاب باز شدن کارت‌ها (برق زدن، لرزش کارت، افکت‌های نوری لجندری).
*   **بازار نقل‌وانتقالات:** طراحی رابط کاربری مشابه بخش Transfer Market بازی فیفا/PES با قابلیت فیلتر قیمت و پست.
*   **سیستم استریم زنده:** Embed کردن پلیر آپارات در داشبورد + ایجاد دکمه «درخواست تعویض لایو» برای مربی.

---

## 🚀 فاز ۴: بهینه‌سازی، امنیت و انتشار اپلیکیشن (Polish & Release)
**هدف:** رفع تمام باگ‌ها، اتصال سرویس‌های اطلاع‌رسانی و ساخت فایل APK.

### بخش بک‌اند و DevOps
*   **Sentry Integration:** راه‌اندازی مانیتورینگ آنلاین برای ردیابی باگ‌ها قبل از کرش کردن سرور.
*   **ربات‌های نوتیفیکیشن:** اتصال بک‌اند به تلگرام/روبیکا (از طریق وب‌هوک یا ابزاری مثل n8n) برای ارسال پیام‌های اتوماتیک نتایج و خریدهای بزرگ بازار.
*   **تست نفوذ و بار (Load Testing):** تست کردن درگاه بانکی و سیستم ترکیب برای اطمینان از عملکرد در فشار بالا.

### بخش موبایل (App Build)
*   **بهینه‌سازی PWA:** اطمینان از عملکرد صحیح آفلاین و کش شدن عکس‌های بازیکنان برای سرعت بیشتر.
*   **Capacitor / TWA:** رپ (Wrap) کردن سایت در یک پوسته اندرویدی، کامپایل فایل خروجی APK و آماده‌سازی کلیدهای امضا (Keystore) برای تاییدیه مایکت و کافه‌بازار.
*   **انتشار:** آپلود و انتشار نهایی در استورها.
