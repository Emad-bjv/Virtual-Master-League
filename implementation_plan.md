# مستر لیگ مجازی - برنامه پیاده‌سازی فنی (فاز MVP)

این سند معماری فنی و برنامه اجرایی فاز اول (MVP) پروژه مستر لیگ مجازی را بر اساس تمام توافقات و مصاحبه‌های استراتژیک (از جمله فایل `review_results.md` و Q&A نهایی) تشریح می‌کند. هدف از این فاز، ایجاد هسته اصلی پلتفرم است.

## معماری کلی سیستم (Tech Stack)

برای تضمین پایداری، مقیاس‌پذیری و جلوگیری از هرگونه باگ یا کرش (مطابق با خواسته شما)، معماری زیر انتخاب شده است:

*   **فرانت‌اند (رابط کاربری):** React.js (Vite) + Tailwind CSS
    *   **انیمیشن‌ها:** Framer Motion (سبک و روان در مرورگر)
    *   **مدیریت ترکیب:** `dnd-kit` برای Drag & Drop مهره‌ها در زمین شماتیک
    *   **خروجی اندروید:** PWA و تبدیل به APK از طریق TWA (Capacitor) برای انتشار در مایکت/بازار.
*   **بک‌اند (سرور):** Python + Django + Django REST Framework
    *   **دیتابیس اصلی:** PostgreSQL (تراکنش‌های اتمیک برای حفظ بودجه و دارایی‌ها)
    *   **پردازش پس‌زمینه (حیاتی):** Celery + Redis (برای محاسبه سنگین خستگی بازیکنان، افت/رشد و مچ‌میکینگ بدون درگیر کردن کاربر)
*   **مانیتورینگ:** Sentry (برای ثبت و رفع لحظه‌ای خطاها و جلوگیری از کرش)

---

## ویژگی‌های کلیدی فاز ۱ (MVP)

در این فاز ما هسته اصلی بازی را توسعه می‌دهیم:

### ۱. سیستم احراز هویت
*   ثبت‌نام/ورود فقط با شماره موبایل و کد یکبار مصرف (OTP - سرویس کاوه‌نگار).
*   محدودیت سفت‌وسخت: هر شماره معادل یک تیم.

### ۲. داشبورد مربی و پنل تاکتیک (Game Plan Clone)
*   **زمین شماتیک:** شبیه‌سازی دقیق زمین PES با ابعاد واقعی.
*   **Drag & Drop:** قابلیت کشیدن بازیکنان روی زمین. پست بازیکن به صورت خودکار بر اساس مختصات (X, Y) تغییر می‌کند (مثلا از CMF به AMF).
*   **منوهای تاکتیکی:** فرم‌های Advanced Instructions و Attacking/Defending دقیقا مشابه بازی PES 2021 پیاده‌سازی می‌شوند تا ادمین به راحتی آن‌ها را به بازی منتقل کند.
*   **استخراج گرافیک:** مینی‌فیس‌ها و لوگوها با نرم‌افزارهای CPK Unpacker مستقیماً از بازی استخراج و در سایت بارگذاری می‌شوند.

### ۳. پنل ادمین اختصاصی (قلب تپنده بازی)
*   ادمین ترکیب نهایی مربیان را می‌بیند.
*   **ثبت وقایع:** فرم ثبت نتیجه، گلزنان، پاس‌گل، کارت‌ها و مصدومیت‌ها در پایان هر بازی.
*   **اتوماسیون:** با ثبت نتیجه توسط ادمین، سیستم به صورت خودکار بودجه‌ها را واریز و فرمول خستگی را برای بازیکنان حاضر در زمین (توسط Celery) اعمال می‌کند.
*   **تعویض Live:** نمایش پنل اختصاصی برای ادمین که درخواست‌های تعویض مربیان را حین پخش زنده (از طریق اتصال آپارات) دریافت می‌کند.

### ۴. بازار و اقتصاد پایه
*   سیستم پایگاه داده قدرتمند برای نگهداری دارایی تیم (دلار مجازی).
*   قابلیت خرید بسته‌های دلاری (پرداخت واقعی با درگاه زرین‌پال - تا سقف ۵۰۰ هزار تومان در هفته).

---

## ساختار پایگاه داده (Entity Relationship)

مدل‌های اصلی (Models) در جنگو به این شکل طراحی خواهند شد:

1.  **User/Manager:** مشخصات مربی، شماره موبایل، کیف پول دلاری.
2.  **Team:** اطلاعات باشگاه (نام، لوگو)، لینک شده به یک مربی.
3.  **Player:** دیتابیس کامل بازیکنان (نام، اورال، سن، استقامت پایه در PES، وضعیت خستگی، تیم فعلی).
4.  **Match:** اطلاعات بازی (تیم میزبان، تیم میهمان، تاریخ، نتیجه، وضعیت: انجام نشده/درحال برگزاری/پایان یافته).
5.  **MatchEvent:** اتفاقات بازی (گل، کارت، تعویض، دقایق حضور) - محاسبه خستگی و رشد/افت از روی این جدول انجام می‌شود.

---

## طرح احراز صحت (Verification Plan)

پس از پیاده‌سازی فاز MVP، سیستم با روش‌های زیر تست می‌شود:

### تست‌های خودکار (Automated Tests)
*   نوشتن Unit Test در جنگو برای فرمول **Virtual Stamina** (اطمینان از اینکه فرمول ریاضی با ضرایب سن و بازی‌های متوالی دقیق کار می‌کند).
*   تست Atomic Transactions (اطمینان از اینکه در صورت قطع شدن اینترنت موقع خرید بسته، پول کاربر از بین نمی‌رود).

### تست‌های دستی (Manual Verification)
*   اجرای یک سناریوی کامل (ثبت نام مربی، ساخت ترکیب با گوشی موبایل، ثبت نتیجه توسط ادمین و مشاهده کسر استقامت).
*   تست رابط کاربری Game Plan در سایزهای مختلف صفحه (ریسپانسیو) تا مربی بتواند با گوشی ترکیب بچیند.

> [!IMPORTANT]
> برای شروع پیاده‌سازی این برنامه، باید زیرساخت اولیه کدها (نصب جنگو و ریکت) انجام شود. لطفاً در صورت تایید این معماری و برنامه، دکمه Proceed را بزنید تا وارد فاز تولید کدهای پایه شویم.
